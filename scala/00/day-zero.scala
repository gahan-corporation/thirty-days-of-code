import scala.io._

object dayZero {
    def main(args: Array[String]) {
        // Print "Hello, World."
        println("Hello, World.")

        // Read a string variable
        val s = readLine() 

        // Print the value of the string variable
        println(s)
    }
}
