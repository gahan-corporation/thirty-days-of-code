#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
  int read_int = 4;
  double read_dec = 8;
  char read_str[] = "Run's house";

  double add_int; 
  double add_dec;
  char add_str[];

  scanf("%[^\n]", read_int); 
  scanf("%[^\n]", read_dec); 
  scanf("%[^\n]", read_str); 
}
